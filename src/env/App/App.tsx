import type {FC} from 'react';
import Calendar from '@/lib/calendar/Calendar';
import "@/index.css"
const App: FC = () => {
    return (
        <div className="">
        <Calendar/>
        </div>
    );
};

export default App;
